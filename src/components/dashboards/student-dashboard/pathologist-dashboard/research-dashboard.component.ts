import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * @deprecated This component is misplaced, obsolete, and has been neutralized.
 * It was located in a confusing directory path (`student-dashboard/pathologist-dashboard/`)
 * and is not used by the application. This file should be deleted.
 */
@Component({
  selector: 'app-research-dashboard-obsolete-final',
  standalone: true,
  imports: [CommonModule],
  template: '<!-- MISPLACED AND OBSOLETE -->',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ResearchDashboardOldComponent {
  // This component is obsolete.
}
