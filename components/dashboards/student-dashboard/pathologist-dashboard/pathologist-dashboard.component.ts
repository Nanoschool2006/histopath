import { ChangeDetectionStrategy, Component } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * @deprecated This component is in a confusing, deeply-nested path and is obsolete.
 * It was causing a class name collision and has been neutralized to prevent build conflicts.
 */
@Component({
  selector: 'app-pathologist-dashboard-obsolete-2',
  standalone: true,
  imports: [CommonModule],
  template: '<!-- OBSOLETE - MOVED -->',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class PathologistDashboardObsoleteComponent2 {
  // This component's logic is obsolete. This file should be deleted.
}
